<html>
<head>
<link rel="stylesheet" href="Makersbnb_main_style.css">
<style>
body {
  background-color: lightblue;
}
h3 {
    text-align: center;
    color: rgb(0, 0, 0);
    text-align: center;
    font-family: verdana;
}
li {
  text-align: center;
  color: rgb(0, 0, 0);
    text-align: center;
    font-family: verdana;
}
</style>
</head>
<h3> List of spaces </h3>

<ul>
  <% @spaces.each do |space| %>
  <p class="space" id="space">
    <%= space.title %>
    <br>
    £<%= space.price %>
    <br>
    <%= space.description %>
    <br>
      <% if space.available? %>
        <form action='booking/<%= space.id %>/new'>
          <input type='submit' value='Available'>
        </form>
      <% else %>
        Booked
      <% end %>
  </li>
  <%end%>
</ul>
  <form action='/spaces/new'>
  <input type='submit' name='Add_spaces' value='Add space'>
  </form>
</html>
